
Процедура ПриКопировании(ОбъектКопирования)
	Дата = ТекущаяДата();
	РаботаСМоделью.Инициализировать(ЭтотОбъект);
	РаботаСМодельюКлиентСервер.РассчитатьПроизводныеПараметры(ЭтотОбъект);// рассчитать не сохраняемые параметры
	РаботаСМоделью.ОбновитьДанные(ЭтотОбъект);//  проверить и рассчитать сохраняемые параметры
КонецПроцедуры
